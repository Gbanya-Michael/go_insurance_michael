{
  "name": "go-travel-insurance",
  "version": "1.0.0",
  "description": "Travel insurance quote application - Tailwind CSS build configuration for development and production",
  "private": true,
  "scripts": {
    "build:css": "tailwindcss --minify -i ./app/assets/tailwind/application.css -o ./app/assets/builds/tailwind.css",
    "watch:css": "onchange -e \"app/assets/builds/**\" -i \"app/**/*.css\" \"app/**/*.html\" \"app/**/*.erb\" \"app/**/*.rb\" \"vendor/**/*.css\" -- yarn build:css",
    "postinstall": "echo 'Run yarn build:css to compile Tailwind CSS, or yarn watch:css for development'"
  },
  "devDependencies": {
    "onchange": "^7.1.0",
    "tailwindcss": "^4.1.16"
  },
  "engines": {
    "node": ">=18.0.0",
    "yarn": ">=1.22.0"
  }
}

